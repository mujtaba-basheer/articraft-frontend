// import { swaggerEndpoint, swaggerSchema } from './utils'

type SwaggerJson = any;

export function applyLoopTrackUpdates(swagger: SwaggerJson) {
  // const schemas = swagger.components.schemas;

  return swagger;
}

export function applyManagerV2Updates(swagger: SwaggerJson) {
  // const schemas = swagger.components.schemas
  // const paths = swagger.paths
  return swagger;
}

export function applyManagerV3Updates(swagger: SwaggerJson) {
  // const schemas = swagger.components.schemas
  // const paths = swagger.paths
  return swagger;
}
